#include <stdio.h>

int main() {
    int T[6],n=0;
    for (int i=0;i<6;i++){
        printf ("entrer l'element :");
        scanf ("%d",T[i]);
    }
   
    for (int i=0;i<6;i++){
       if(int element == T[i];)
        
       
       
        
        
    }

    return 0;
}