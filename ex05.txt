#include <stdio.h>

int main() {
   int T[6];
   for (int i=0;i<6;i++){
       printf ("entrer :");
       scanf ("%d",&T[i]);
   }
   int min=T[0];
 for (int i=0;i>6;i++){
     if (T[i] > min);
     min=T[i];
 }
 printf ("e nombre minimun est :%d",min);
 
    return 0;
}